package be.intersentia.elasticsearch.configuration.annotation.mapping;

/**
 * This enum can hold the true and false boolean states, together with a third DEFAULT state. The function of the
 * DEFAULT state is context-specific.
 */
public enum OptionalBoolean {
    TRUE, FALSE, DEFAULT
}
